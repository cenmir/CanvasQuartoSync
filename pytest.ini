[pytest]
testpaths = tests
markers =
    quarto: tests requiring Quarto CLI
    canvas: tests requiring Canvas API credentials and a real course
    slow: tests taking more than a few seconds
addopts = -v --tb=short
