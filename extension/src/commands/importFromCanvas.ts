import * as vscode from 'vscode';

// Placeholder — will be implemented in Phase 4
export async function importFromCanvas(): Promise<void> {
  vscode.window.showInformationMessage(
    'Import from Canvas is under development.'
  );
}
